      - name: Organize APK Artifacts
        run: |
          mkdir -p apk_outputs
          find . -name "*.apk" -not -path "*/apk_outputs/*" -exec cp -v {} apk_outputs/ \;
          ls -la apk_outputs/

      - name: Upload APK Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DigitBoard-APKs
          path: apk_outputs/**     # <--- O segredo do v4 é usar /** para incluir todos os arquivos da pasta

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="v1.0.0-build-${{ github.run_number }}"
          git tag -f "$TAG_NAME" || true
          git push origin "$TAG_NAME" -f || true
          gh release create "$TAG_NAME" apk_outputs/*.apk \
            --title "DigitBoard Virtual Keyboard $TAG_NAME" \
            --notes "Compilação automática de APK realizada com sucesso via GitHub Actions CI/CD."        run: gradle :app:assembleDebug --stacktrace
        continue-on-error: true

      - name: Build Release APK
        run: gradle :app:assembleRelease --stacktrace || echo "Release build step finished"
        continue-on-error: true

      - name: Organize APK Artifacts
        run: |
          mkdir -p apk_outputs
          find . -name "*.apk" -not -path "*/apk_outputs/*" -exec cp -v {} apk_outputs/ \; || true
          ls -la apk_outputs/

      - name: Upload APK Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DigitBoard-APKs
          path: apk_outputs/

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="v1.0.0-build-${{ github.run_number }}"
          git tag -f "$TAG_NAME" || true
          git push origin "$TAG_NAME" -f || true
          gh release create "$TAG_NAME" apk_outputs/* \
            --title "DigitBoard Virtual Keyboard $TAG_NAME" \
            --notes "Compilação automática de APK realizada com sucesso via GitHub Actions CI/CD." || echo "Release step finished"
